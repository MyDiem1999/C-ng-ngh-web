@using DoAn.Models
@model List<tbl_Nhom>

<div>
    @foreach (var sp in Model)
    {
        <div>
            <a class="dropdown-item" href="@Url.Action("Locds", new {ThuocT="nhomm",ma=@sp.MaNhom })">@sp.TenNhom</a>
            <div class="dropdown-divider"></div>          
        </div>
    }

</div>